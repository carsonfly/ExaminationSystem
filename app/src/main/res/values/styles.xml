<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme">
        <item name="foregroundColorValue">@color/dayForeground</item>
        <item name="backgroundColorValue">@color/dayBackgroud</item>
    </style>
    <style name="NightTheme" parent="android:Theme">
        <item name="foregroundColorValue">@color/nightForeground</item>
        <item name="backgroundColorValue">@color/nightBackground</item>
    </style>
</resources>
